---
phase: 3
slug: dashboard-cashout-flow
status: approved
reviewed_at: 2026-05-10
shadcn_initialized: true
preset: none
created: 2026-05-10
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the dashboard and cashout flow.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (new-york style) |
| Preset | none (manually configured) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Geist Sans (sans), Geist Mono (mono) |
| Tailwind version | v4 |
| Base color | slate |
| CSS variables | true |

**Source:** `components.json` + `npx shadcn info`

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap, inline badge padding |
| sm | 8px | Within-card element spacing, button icon gaps |
| md | 16px | Default element spacing, card content padding supplement |
| lg | 24px | Card gap, section header-to-content |
| xl | 32px | Between major sections (stats row → trade list) |
| 2xl | 32px | Page vertical padding (`py-8`); section spacing (`mt-8`) |
| 3xl | 48px | Not used in this phase |

Exceptions:
- Touch targets for icon-only buttons (Settings, collapse chevron): minimum 44px hit area via `p-3` wrapper (12px padding; 20px icon + 24px total padding = 44px)
- Stats cards grid gap: `gap-6` (24px) — matches existing dashboard pattern
- Page horizontal padding: `px-4` (16px) on mobile, auto margins on `max-w-4xl` container on desktop

**Source:** Extracted from existing `dashboard/page.tsx` patterns; standard 8-point scale applied.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Display | 30px (text-3xl) | 600 (semibold) | 1.2 | SOL balance amounts in stat cards |
| Heading | 20px (text-xl) | 600 (semibold) | 1.2 | Section headings ("Recent Trades"), modal title |
| Body | 16px (text-base) | 400 (normal) | 1.5 | USD amounts, breakdown panel labels and values |
| Label | 14px (text-sm) | 400 (normal) | 1.5 | Wallet address (truncated), card metadata, tier subtitle, badge text (tier label, "Loss", "Cashed Out"), ROI/P&L inline chips |

**Weight declarations (exactly 2):** regular 400 + semibold 600. No other weights used.

**Font stack:**
- Primary: `var(--font-geist-sans)` via `font-sans` — all body and UI text
- Monospace: `var(--font-geist-mono)` via `font-mono` — token mint addresses, wallet addresses, SOL amounts in cashout modal

**Source:** Existing `dashboard/page.tsx` established `text-3xl font-bold`, `text-xl font-semibold`, `text-lg`, `text-sm`. Normalized to exact 4-size scale. Micro (12px) merged into Label (14px/text-sm); bold (700) and medium (500) weights eliminated — all display numerics use semibold (600).

---

## Color

### Base Palette (dark mode — enforced globally)

| Role | Tailwind Class | Hex Approx | Usage |
|------|---------------|-----------|-------|
| Dominant surface (60%) | `bg-black` | #000000 | Page background, full-screen container |
| Secondary surface (30%) | `bg-zinc-950` | #09090b | Stat cards, trade cards, modal background |
| Tertiary surface | `bg-zinc-900` | #18181b | Button backgrounds, expanded breakdown panel, input backgrounds |
| Border | `border-zinc-800` | #27272a | Card borders, dividers |
| Text primary | `text-white` | #ffffff | SOL amounts, section headings, modal headings |
| Text secondary | `text-zinc-300` | #d4d4d8 | Card titles ("Trading Wallet", "Vault Wallet") |
| Text muted | `text-zinc-400` | #a1a1aa | Subtitle text, USD amounts, body descriptors |
| Text subtle | `text-zinc-500` | #71717a | Wallet addresses (truncated), disabled states |

### Accent (10%) — Reserved-For List

Accent is reserved EXCLUSIVELY for these elements:

| Element | Color | Value |
|---------|-------|-------|
| Primary action buttons ("Cash Out", "Confirm Cashout") | `bg-white text-black` | White on black — high contrast CTA |
| Sync button hover state | `hover:text-white` | White text on zinc-800 hover |
| Positive P&L value | `text-green-400` | #4ade80 |
| Negative P&L value | `text-red-400` | #f87171 |
| Copy-to-clipboard success state | `text-green-400` | #4ade80 |

### Tier Color System (semantic — from `getTierColor()`)

These colors appear ONLY in tier badges and tier card accent elements:

| Tier | Label | Color | Hex | Tailwind Approx |
|------|-------|-------|-----|-----------------|
| Tier 1 (REBUILD) | Critical | Red | #ef4444 | `red-500` |
| Tier 2 (RECOVERY) | Caution | Orange | #f97316 | `orange-500` |
| Tier 3 (GROWTH) | Stable | Green | #22c55e | `green-500` |
| Tier 4 (AGGRESSIVE) | Healthy | Blue | #3b82f6 | `blue-500` |
| Tier 5 (MAXIMUM) | Peak | Purple | #8b5cf6 | `violet-500` |

Tier colors are applied via inline `style={{ backgroundColor: color, borderColor: color }}` on Badge component (not Tailwind classes) because they come from `getTierColor()` at runtime.

### Destructive

| Role | Class | Usage |
|------|-------|-------|
| Destructive | `text-red-400` / `border-red-800` | No destructive actions in this phase (cashout confirmation is constructive — user is logging a transfer they already made) |

**Source:** Existing `dashboard/page.tsx` color patterns (`bg-black`, `bg-zinc-950`, `border-zinc-800`). Tier colors from `src/lib/tier-calculator.ts`. CONTEXT.md confirmed tier color/label mapping.

---

## Component Inventory

All components already installed. No new shadcn installs required for this phase.

### Existing shadcn Components (reuse as-is)

| Component | File | Phase 3 Usage |
|-----------|------|---------------|
| `Card`, `CardHeader`, `CardTitle`, `CardContent` | `@/components/ui/card` | Stat cards (trading wallet, vault wallet, tier), trade cards |
| `Badge` | `@/components/ui/badge` | Tier label badge, "Cashed Out" chip, "Loss" chip — with className overrides for tier colors |
| `Button` | `@/components/ui/button` | "Refresh Trades", "Cash Out", "Confirm Cashout", "Go Back", copy button |
| `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter` | `@/components/ui/dialog` | Cashout confirmation modal |
| `Skeleton` | `@/components/ui/skeleton` | Loading state for trade list and balance cards |
| `Progress` | `@/components/ui/progress` | Monthly goal progress indicator (minimal — DASH-06) |

### New Dashboard Components (to create)

| Component | Path | Responsibility |
|-----------|------|----------------|
| `TierBadge` | `src/components/dashboard/TierBadge.tsx` | Color-coded tier badge + label + subtitle |
| `StatsRow` | `src/components/dashboard/StatsRow.tsx` | 3-col grid: tier card + trading balance card + vault balance card |
| `TradeList` | `src/components/dashboard/TradeList.tsx` | List of TradeCard components with section header + sync button |
| `TradeCard` | `src/components/dashboard/TradeCard.tsx` | Collapsed: token, P&L, ROI, cashout amount; expands to CashoutBreakdown |
| `CashoutBreakdown` | `src/components/dashboard/CashoutBreakdown.tsx` | Expanded panel: tier/bonus/streak/boost breakdown + "Cash Out" button |
| `CashoutModal` | `src/components/dashboard/CashoutModal.tsx` | Confirmation dialog: amount, vault address with copy, confirm/go back |
| `GoalProgress` | `src/components/dashboard/GoalProgress.tsx` | Minimal goal progress line (DASH-06 scope only) |

### Icons (lucide-react — already installed)

| Icon | Usage |
|------|-------|
| `RefreshCw` | Sync / Refresh Trades button |
| `Loader2` | Spinning loading state during sync and cashout confirm |
| `ChevronDown` / `ChevronUp` | Trade card expand/collapse toggle |
| `Copy` | Copy vault address button (idle state) |
| `Check` | Copy vault address button (success state, 2s) |
| `TrendingUp` | Winning trade P&L indicator |
| `TrendingDown` | Losing trade P&L indicator |
| `Settings` | Settings button (already in header) |
| `LogOut` | Disconnect button (already in header) |

**Source:** `lucide-react ^0.562.0` in `package.json`; icons verified available in this version.

---

## Layout Specification

### Dashboard Page Layout

```
[Page: bg-black, min-h-screen, px-4 py-8]
  [Container: mx-auto max-w-4xl]
    [Header row: flex justify-between items-center mb-8]
      [Title: "Dashboard" h1 text-3xl font-semibold text-white]
      [Subtitle: text-zinc-400 text-sm mt-1]
      [Actions: flex gap-2 — Refresh Trades, Settings, Disconnect]

    [StatsRow: grid grid-cols-1 md:grid-cols-3 gap-6 mb-8]
      [TierCard: Card bg-zinc-950 border-zinc-800 — WIDER prominence]
        [Tier number + TierBadge + subtitle]
        [Losing streak count (if > 0): subtle warning below badge]
      [TradingWalletCard: Card bg-zinc-950 border-zinc-800]
        [SOL balance text-3xl font-semibold text-white]
        [USD balance text-base text-zinc-400]
        [Truncated address text-sm text-zinc-500 font-mono]
      [VaultWalletCard: Card bg-zinc-950 border-zinc-800]
        [SOL balance text-3xl font-semibold text-white]
        [USD balance text-base text-zinc-400]
        [Truncated address text-sm text-zinc-500 font-mono]

    [TradeList section]
      [Section header: flex justify-between items-center mb-4]
        [h2: "Recent Trades" text-xl font-semibold text-white]
        [Refresh Trades button: outline, border-zinc-700 bg-zinc-900]
      [GoalProgress: single line progress indicator — "Goal: $X / $400 this month"]
      [TradeCard list: flex flex-col gap-3 mt-8]
        [TradeCard × up to 10]
```

### StatsRow — Tier Card Treatment

The tier card spans the same width as each balance card in a 3-column grid (`md:grid-cols-3`). The tier is visually differentiated by having the tier badge as the primary visual element at the top, with the tier number displayed large (`text-3xl font-semibold`) and the tier color applying as a colored left border (`border-l-4 style={{ borderColor: tierColor }}`).

### Trade Card Layout

**Collapsed (default):**
```
[Card: bg-zinc-950 border-zinc-800 rounded-lg]
  [clickable header: w-full flex justify-between items-center p-4]
    [Left: token name/mint truncated, font-mono text-sm text-zinc-300]
    [Right: P&L chip + ROI chip + cashout amount OR badge]
    [Chevron: text-zinc-500, rotates 180deg when expanded]
  [For loss trades: opacity-50 on entire card]
```

**Expanded (breakdown visible):**
```
[Card — same as collapsed]
  [clickable header — same]
  [Divider: border-t border-zinc-800]
  [CashoutBreakdown panel: p-4 bg-zinc-900 rounded-b-lg]
    [Breakdown table: 4 rows]
      [Tier base rate: "Tier {N} base" — {X}%]
      [ROI bonus: "ROI bonus ({roi}%)" — +{X}%]
      [Streak multiplier: "Streak ×{N}" — ×{X}]
      [Goal boost: "Goal boost" — +{X}%]
      [Divider]
      [Final: "Take off the table" font-semibold — {X} SOL ({Y}%)]
    [Cash Out button: w-full mt-8, variant default (white bg, black text)]
```

### Cashout Modal

```
[Dialog overlay: fixed inset-0 bg-black/80]
  [DialogContent: bg-zinc-950 border-zinc-800 max-w-md]
    [DialogHeader]
      [DialogTitle: "Confirm Cashout" text-white]
      [Description: "Transfer SOL to your vault wallet, then confirm below." text-zinc-400 text-sm]
    [Body: space-y-4 py-8]
      [Amount row: flex justify-between items-center]
        [Label: "Amount" text-zinc-400 text-sm]
        [Value: "{X.XXXX} SOL" text-white font-semibold font-mono text-base]
      [Vault address row: flex flex-col gap-2]
        [Label: "Vault wallet" text-zinc-400 text-sm]
        [Address + copy button: flex items-center gap-2 bg-zinc-900 rounded p-3]
          [Address: font-mono text-sm text-zinc-300 flex-1 break-all]
          [Copy button: icon-only, 36px, border-zinc-700, aria-label="Copy vault address"]
    [DialogFooter: flex gap-2 mt-8]
      [Go Back: variant outline, border-zinc-700 text-zinc-400]
      [Confirm Cashout: variant default (white bg), disabled + Loader2 while pending]
```

---

## Interaction Contracts

### Trade Card Expand/Collapse
- Toggle on click anywhere in the collapsed header row
- `ChevronDown` rotates to `ChevronUp` (CSS `transition-transform duration-200`)
- Only one card can be expanded at a time (each card manages own `useState(false)`)
- Expansion: instant (no animation required for MVP)

### Copy Vault Address
- Click `Copy` icon button (`aria-label="Copy vault address"`)
- `navigator.clipboard.writeText(vaultAddress)` fires
- Icon switches from `Copy` to `Check` (green) for 2 seconds via `setTimeout`
- No toast — the inline visual feedback is sufficient

### Cashout Confirmation
1. User clicks "Cash Out" inside expanded breakdown panel
2. `CashoutModal` opens (`Dialog` controlled via `open` + `onOpenChange`)
3. User reads amount + vault address, copies address if needed
4. User clicks "Confirm Cashout"
5. Button enters loading state: disabled + `Loader2 animate-spin` + text "Confirming..."
6. `useMutation(confirmCashout)` resolves:
   - **Success:** Modal closes, `toast.success("Cashout confirmed — {X.XXXX} SOL logged")`, trade card shows "Cashed Out" badge, `useQuery` auto-updates wallet balances
   - **Error:** Modal stays open, `toast.error("Failed to confirm cashout. Try again.")`, button returns to active state
7. User clicks "Go Back" to dismiss modal without confirming

### Sync Button
- Exists in both header area (original) and Trade List section header
- Keep in section header only (contextually tied to trade list per CONTEXT.md decision)
- Remove from top header area in this phase
- Loading: `Loader2 animate-spin` + "Syncing..." text, button disabled
- Success/error: existing toast patterns from Phase 2.1

### Loading States
- Wallets `undefined`: render full-page skeleton (`min-h-screen bg-black flex items-center justify-center`)
- Trades `undefined`: render `Skeleton` components in place of trade cards (3 skeleton cards, each `h-16 rounded-lg bg-zinc-900`)
- Balance cards loading: `Skeleton` in place of SOL amount (`h-8 w-32 bg-zinc-900`)

### Losing Streak Display
- Surface streak count on the tier card if `currentLosingStreak > 0`
- Format: `"⚠ {N}-loss streak"` in `text-sm text-orange-400` below the tier badge subtitle
- Hide entirely when streak = 0 (no positive reinforcement for 0 streak — it is the default)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (cashout initiation) | "Cash Out" |
| Primary CTA (cashout confirmation) | "Confirm Cashout" |
| Cashout cancel | "Go Back" |
| Empty state heading (no trades) | "No trades yet" |
| Empty state body | "Sync your wallet to detect closed trades and get cashout recommendations." |
| Empty state CTA | "Refresh Trades" |
| Sync success (new trades) | "Found {N} new closed trade{s} (+{X.XXXX} SOL)" |
| Sync success (no new trades) | "No new closed trades found" |
| Sync error | "Failed to sync trades. Try again." |
| Cashout confirm success | "Cashout confirmed — {X.XXXX} SOL logged" |
| Cashout confirm error | "Failed to confirm cashout. Try again." |
| Copy success (inline) | No toast — `Check` icon feedback only |
| Tier subtitle — Critical (1) | "Fear, overtrading zone" |
| Tier subtitle — Caution (2) | "Getting stable" |
| Tier subtitle — Stable (3) | "Balanced, confident" |
| Tier subtitle — Healthy (4) | "At comfortable size" |
| Tier subtitle — Peak (5) | "Above target capacity" |
| Losing streak warning | "{N}-loss streak" |
| Loss trade label | "Loss" |
| Confirmed trade label | "Cashed Out" |
| Goal progress | "Goal: ${X} / ${Y} this month" |
| Cashout modal title | "Confirm Cashout" |
| Cashout modal description | "Transfer SOL to your vault wallet, then confirm below." |
| Dashboard page title | "Dashboard" |
| Dashboard page subtitle | "Your wallet balances and profit tracking" |
| Loading state | "Loading dashboard..." |

**Destructive actions:** None in this phase. Cashout confirmation is constructive (logging a manual transfer the user already performed). No confirm-to-delete patterns needed.

**Source:** CONTEXT.md confirmed tier labels and descriptions match existing `TIER_CONFIG` constants. Empty state and error copy uses existing project pattern (imperative, problem + solution).

---

## State Catalog

Every UI element that has multiple states must be explicitly handled:

### Trade Card States

| State | Visual Treatment |
|-------|-----------------|
| Pending win (cashout available) | Full opacity, shows recommended cashout amount in yellow-400, "Cash Out" button available in breakdown |
| Confirmed win (cashout logged) | Full opacity, "Cashed Out" badge (bg-green-900 text-green-300) replaces amount, no "Cash Out" button |
| Loss | `opacity-50` on entire card, "Loss" badge (outline variant, text-red-400), no "Cash Out" button, no expand |

### Wallet Balance Card States

| State | Visual Treatment |
|-------|-----------------|
| Loading | `Skeleton` h-8 w-32 for SOL amount, h-5 w-20 for USD |
| Loaded | Render SOL + USD values |
| After cashout confirm | `useQuery` auto-updates — no special state needed |

### Cashout Modal States

| State | Visual Treatment |
|-------|-----------------|
| Idle | "Confirm Cashout" button active, white bg, black text; "Go Back" button available |
| Confirming | "Confirm Cashout" button disabled, `Loader2 animate-spin`, text "Confirming..."; "Go Back" button disabled |
| Success | Modal closes, toast fires |
| Error | Modal stays open, toast fires, button resets |

### Tier Card States

| State | Visual Treatment |
|-------|-----------------|
| Loading | Skeleton h-10 w-24 for tier number |
| Tier 1–5 | Color from `getTierColor(tier)` applied to left border + badge background |
| Losing streak = 0 | Streak line hidden |
| Losing streak > 0 | `"{N}-loss streak"` in text-sm text-orange-400 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Badge, Button, Dialog, Skeleton, Progress | Not required — official registry |
| Third-party | None | Not applicable |

No third-party registries declared for this phase. All required components are already installed from the official shadcn registry or were installed in prior phases.

**Confirmed:** `components.json` shows `"registries": {}` — no third-party registries configured.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
