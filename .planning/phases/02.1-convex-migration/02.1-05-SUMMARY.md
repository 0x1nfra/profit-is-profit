---
phase: 02.1-convex-migration
plan: "05"
subsystem: database
tags: [supabase, convex, cleanup, migration, env]

# Dependency graph
requires:
  - phase: 02.1-convex-migration/02.1-04
    provides: Convex sync action + reactive dashboard — all functionality migrated from Supabase to Convex
provides:
  - Zero Supabase dependency; all DB logic runs on Convex
  - Clean build with no @supabase imports anywhere in src/
  - trades/refresh API route stubbed as 410 (sync moved to Convex action)
  - health-check page updated to Convex env checks
  - Supabase env vars removed from .env.local
affects: [03-cashout, all future phases]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Deprecated HTTP routes get 410 stubs with comment pointing to replacement (Convex action)"
    - "Dead service files replaced with deprecation notice + export {} to satisfy TypeScript"

key-files:
  created: []
  modified:
    - package.json (removed @supabase/ssr and @supabase/supabase-js)
    - src/app/api/trades/refresh/route.ts (replaced with 410 stub)
    - src/app/health-check/page.tsx (removed Supabase imports, added Convex env checks)
    - src/lib/services/trade-service.ts (replaced with deprecation notice)
    - .env.local (removed NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY)
  deleted:
    - src/lib/supabase.ts
    - src/types/database.ts
    - src/migrations/0001_initial.sql

key-decisions:
  - "trade-service.ts replaced with deprecation stub rather than deleted — keeps file history readable and avoids potential import ghosts"
  - "health-check page rewritten to check Convex env vars instead of Supabase (NEXT_PUBLIC_CONVEX_URL, JWT_PRIVATE_KEY, etc.)"
  - "pnpm used (not npm) — project uses pnpm-lock.yaml; npm install throws arborist null error on pnpm symlinks"

patterns-established:
  - "Deprecated API routes: 410 status + comment pointing to Convex replacement"
  - "Deprecated service files: export {} stub with JSDoc deprecation notice"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-02-23
---

# Phase 02.1 Plan 05: Supabase Removal Summary

**Complete Supabase removal: packages uninstalled, all files deleted, env vars cleaned, trades/refresh stubbed as 410, build and TypeScript pass clean**

## Performance

- **Duration:** ~6 min (Task 1 only — Task 2 awaiting human verification)
- **Started:** 2026-02-23T14:43:50Z
- **Completed:** 2026-02-23T14:50:00Z (checkpoint at Task 2)
- **Tasks:** 1 of 2 complete (Task 2 is human-verify checkpoint)
- **Files modified:** 5 modified, 3 deleted

## Accomplishments

- Removed all Supabase packages (`@supabase/supabase-js`, `@supabase/ssr`) from package.json and pnpm lockfile
- Deleted `src/lib/supabase.ts`, `src/types/database.ts`, `src/migrations/0001_initial.sql`
- Cleaned three Supabase env vars from `.env.local` — only Convex vars remain
- Replaced `src/app/api/trades/refresh/route.ts` with 410 stub pointing callers to `api.sync.syncWalletTrades`
- Rewrote `src/app/health-check/page.tsx` to check Convex env vars instead of Supabase
- `npx tsc --noEmit` and `npm run build` both pass with zero errors

## Task Commits

1. **Task 1: Remove all Supabase files, packages, env vars; stub trades/refresh; audit** - `d44344e` (chore)
2. **Task 2: End-to-end flow verification** - PENDING (human-verify checkpoint)

## Files Created/Modified

- `package.json` - Removed @supabase/ssr and @supabase/supabase-js dependencies
- `src/app/api/trades/refresh/route.ts` - Replaced with 410 stub (6 lines, from 140)
- `src/app/health-check/page.tsx` - Rewritten to check Convex env vars, removed supabaseAdmin import
- `src/lib/services/trade-service.ts` - Replaced with deprecation notice (export {}), was 609 lines of Supabase logic
- `.env.local` - Removed NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
- `src/lib/supabase.ts` - DELETED
- `src/types/database.ts` - DELETED
- `src/migrations/0001_initial.sql` - DELETED

## Decisions Made

- trade-service.ts replaced with deprecation stub (`export {}`) rather than deleted outright — keeps git history traceable and avoids phantom import errors if any file was missed
- health-check page rewritten around Convex env vars: `NEXT_PUBLIC_CONVEX_URL`, `NEXT_PUBLIC_CONVEX_SITE_URL`, `JWT_PRIVATE_KEY`, `HELIUS_API_KEY`
- Used `pnpm install` (not `npm install`) — project is pnpm-managed; npm throws an arborist bug with pnpm symlinks

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Rewrote health-check page to remove Supabase imports**
- **Found during:** Task 1 (pre-deletion audit)
- **Issue:** `src/app/health-check/page.tsx` imported from `@/lib/supabase` and referenced SUPABASE env vars directly — would fail to compile after supabase.ts deletion
- **Fix:** Rewrote page to check Convex env vars (`NEXT_PUBLIC_CONVEX_URL`, `NEXT_PUBLIC_CONVEX_SITE_URL`, `JWT_PRIVATE_KEY`, `HELIUS_API_KEY`) instead of Supabase
- **Files modified:** src/app/health-check/page.tsx
- **Verification:** tsc --noEmit clean, build succeeds
- **Committed in:** d44344e (Task 1 commit)

**2. [Rule 1 - Bug] Replaced trade-service.ts with deprecation stub**
- **Found during:** Task 1 (pre-deletion audit)
- **Issue:** `src/lib/services/trade-service.ts` imported from `@/lib/supabase` throughout — would fail to compile after deletion. The file was no longer called anywhere (sync moved to Convex in Plan 04) but still imported the deleted Supabase client
- **Fix:** Replaced entire file with `export {}` + deprecation comment pointing to Convex equivalents
- **Files modified:** src/lib/services/trade-service.ts
- **Verification:** tsc --noEmit clean, build succeeds
- **Committed in:** d44344e (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - Bug: imports from file about to be deleted)
**Impact on plan:** Both required for TypeScript to compile after deleting supabase.ts. No scope creep.

## Issues Encountered

- npm uninstall and npm install both fail on this project due to an arborist bug with pnpm symlinks (`Cannot read properties of null (reading 'matches')`). Resolved by editing package.json directly and running `pnpm install` instead.

## User Setup Required

None — .env.local changes are local only (gitignored). No external service configuration needed. Supabase project can be archived/deleted after human verification confirms the full flow works on Convex.

## Next Phase Readiness

- Phase 2.1 Convex migration is complete pending human end-to-end verification (Task 2 checkpoint)
- After Task 2 approved: Phase 2 UAT can proceed (the original Phase 2 blocker — schema drift and `as any` casts — is now permanently resolved)
- All Convex functions are deployed; trading flow is fully Convex-backed

---
*Phase: 02.1-convex-migration*
*Completed: 2026-02-23 (pending Task 2 human verification)*
